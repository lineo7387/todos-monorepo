# todo-data

This package marks the shared data-access layer for the cross-platform todos product.

Responsibilities:

- Supabase client setup
- Auth/session adapters for web, mobile, and desktop runtimes
- Shared task queries and mutations

Implementation starts in tasks `2.2` and `2.3`.

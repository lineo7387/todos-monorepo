# todo-app

This package marks the shared application layer for the cross-platform todos product.

Responsibilities:

- Authenticated task-loading flow
- Optimistic update orchestration and recovery
- Shared view-model helpers that platform shells can consume

Implementation starts in tasks `3.1` through `3.3`.

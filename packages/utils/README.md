# utils

Shared todo domain utilities for the monorepo.

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp run utils#test
```

- Build the library:

```bash
vp run utils#build
```
